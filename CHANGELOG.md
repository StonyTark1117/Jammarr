# Changelog

## 1.1.0 - Unreleased

- Add a separately pinned Minecraft 1.8.9 Legacy Fabric preview with Java 8 bytecode, protocol-6 optional-client negotiation, commands, player/config screens, direct legacy OpenAL playback, and canonical server configuration/state behavior.
- Add a separately pinned Minecraft 1.6.4 Legacy Fabric preview with Java 8 bytecode and a bounded native custom-payload bridge for protocol-6 negotiation where the Legacy Fabric networking API is unavailable.
- Certify the 1.8.9 Legacy Fabric target with invalid-config rejection, a real incompatible-protocol client, an actual client without Jammarr, two-client audible timing, the full recovery/station scenario matrix, clean process/port teardown, and exact-artifact two-client live-Plex acceptance in the completed 99-runtime matrix.
- Certify the 1.6.4 Legacy Fabric target with fail-closed configuration, protocol mismatch, command/UI, actual no-Jammarr client, synchronized two-client audio, recovery/station scenarios, and clean teardown gates.
- Add and certify a separately pinned Minecraft 1.8.9 Ornithe generation-2 preview with Feather mappings, OSL networking and lifecycle integration, Java 8 bytecode/runtime, optional-client negotiation, command/UI coverage, synchronized two-client audio, recovery scenarios, and clean teardown.
- Add and certify a separately pinned Minecraft 1.6.4 Ornithe generation-2 preview with Feather mappings, only the version-compatible OSL modules Jammarr uses, Java 8 bytecode/runtime, optional-client negotiation, command/UI coverage, synchronized two-client audio, recovery scenarios, and clean teardown.
- Add and runtime-certify a production-reobfuscated Minecraft 1.8.9 LiteLoader client companion, explicitly paired with a Forge server, with protocol-6 negotiation, audio playback, player/config UI coverage, and pinned upstream loader bytes. LiteLoader is not advertised as a dedicated-server runtime.
- Add and runtime-certify a fully obfuscated Minecraft 1.7.10 LiteLoader client companion using checksum-pinned production Forge and LiteLoader distributions. The supported client requires Forge+LiteLoader coexistence and pairs only with the matching Forge server; standalone LiteLoader cannot complete Forge 1.7.10's FML handshake.
- Add and runtime-certify a fully obfuscated Minecraft 1.6.4 LiteLoader client companion using official LiteLoader 1.6.4_01 and recommended Forge 9.11.1.1345. Its Java-7 class headers remain Java-8 runtime code compatible with Forge's ASM 4, and the production pair passes protocol, player/config UI, playable-audio, sustained-health, and teardown gates.
- Expand the candidate matrix to 78 artifacts and 99 dedicated-server loader/version runtimes while keeping Babric, Legacy Fabric, LiteLoader, Ornithe, and Quilt compatibility boundaries explicit.
- Fix deterministic live audio skipping caused by a partially accepted atomic chunk window being retried from its first missing chunk instead of its original server-authorized bounds. Modern and all 11 legacy players now reserve capacity for the complete request, accept already-consumed duplicates during an exact retry, and retain the original bounds until acknowledgement; exact-current Forge 1.7.10 and NeoForge 26.2 two-client live-Plex gates pass without decoder starvation or silence.
- Make the root release matrix enable every manifest-declared LiteLoader paired-client gate instead of leaving those runtime checks opt-in.
- Make global clear operations remove persisted repeat-suppression history on every saved-data implementation, matching the shared store contract and preventing cleared or restart-reset servers from retaining old playback history.
- Deep-certify the final Forge 1.7.10 artifact with twenty cold-client cycles, forty audible sound reloads, all six deterministic impairment profiles, the 30-minute playback fixture, LiteLoader and no-mod client coverage, a second production boot that reloads schema-4 state, and two private-X production clients audibly playing live Plex through the exact DiscPanel candidate.
- Order resource processing before Java compilation in every Forge-family module that intentionally shares its classes/resources output, preventing clean or non-incremental builds from erasing compiled classes before tests and packaging.
- Address the menu-clarity feedback from GitHub issue #2 with concise hover help for every actionable player-menu tab and control across all legacy and modern screen adapters, backed by real private-X hover-render gates for Forge 1.7.10, the 1.18.2 compatibility renderer, and the 1.20.1 native `Tooltip` API.
- Repair the 1.18.2 real-client command acceptance bridge by using that era's `LocalPlayer.chat(String)` fallback, allowing player-visible diagnostics and UI gates to complete instead of timing out before screen validation.
- Add a production Forge 1.7.10 UI regression probe for issue #5 that clicks the real search field and proves typing, backspace, retyping, focus, and cursor state survive repeated screen rebuilds.
- Validate the shared Plex implementation against the live deployment with blank-library preference for `Music`, exclusion of a real item from the separate ASMR library, browse/search/station/Adventure/transcode coverage, and 30 repeat-safe autoplay transitions.
- Add a manifest-driven DiscPanel reconciler with dry-run-by-default planning, exact stopped-state drift checks, sequential no-mod native instance creation, pinned Java images, and fail-closed handling for custom legacy loader distributions.
- Reconcile the live DiscPanel matrix to all 99 manifest-derived runtime profiles, including checksum-pinned Babric and Legacy Fabric launchers plus inspected Ornithe generation-2 bootstrap distributions; leave every profile stopped with a unique port and verify all 78 newly created instances contain no mod records.
- Add a dry-run-by-default DiscPanel release deployer that verifies all 78 indexed artifacts, maps the 75 server artifacts onto all 99 runtime profiles, refuses running, drifted, autostarted, duplicate, or partial deployments, verifies uploaded bytes, and retains the previously active Jammarr file as a disabled rollback without starting a server.
- Diagnose the post-disk-resize 1.18.2 DiscPanel failure as a stale never-started Docker container name conflict, preserve its bind-mounted server data, and clear only the conflicting container so current-candidate runtime validation can resume.
- Stage the exact 1.1.0 release-candidate artifact on all 99 DiscPanel runtime profiles, verify every remote digest, retain 21 stable 1.0.2 rollback files disabled, and finish with no active older Jammarr artifact or deployment-created server start.
- Audit and remove 158 never-started DiscPanel Docker objects without force or volume deletion, proving their writable layers and diffs were empty while preserving all 158 persistent server directories and 5,611 bound files.
- Pin each DiscPanel native runtime to the exact Fabric, Forge, NeoForge, or Quilt loader declared by the release manifest instead of allowing container defaults to drift.
- Add a guarded live-Plex DiscPanel configurator that keeps credentials environment-only, blanks the library setting to exercise the `Music` metadata fallback, preserves existing canonical configs by default, and never starts a server.
- Configure 78 stopped DiscPanel profiles for live-Plex fallback testing while preserving 21 pre-existing configs, then validate the final 1.12.2 Forge artifact through a real Plex connection and clean server stop after recovering from a transient upstream installer timeout.
- Add a dry-run-by-default, single-runtime DiscPanel server smoke gate with exact artifact/hash/loader preflight, post-snapshot log evidence that rejects replayed historical lines, installer-failure classification, and guaranteed stopped-state teardown.
- Make the DiscPanel smoke gate accept both explicit legacy initialization lines and modern Jammarr Plex startup markers after exact remote-artifact preflight, and fail early when a started profile returns to STOPPED before acceptance.
- Recover once when DiscPanel recreates a missing container reference but leaves the replacement stopped, while retaining bounded failure, fresh-log, live-Plex, and guaranteed-teardown requirements; the current-hash 1.18.2 Fabric canary passes this path.
- Add a checksum-pinned, dry-run-by-default DiscPanel runtime-dependency deployer and stage the required StationAPI 2.0.0-alpha.6.2 dependency on the stopped Beta 1.7.3 Babric profile without replacing Jammarr or starting the server.
- Pass fresh final-artifact live-Plex server canaries for Beta 1.7.3 Babric, Forge 1.6.4, Fabric 1.20.1, and NeoForge 26.2 with clean stopped-state teardown; classify the first Babric and Forge attempts as dependency/download bootstrap failures rather than mod failures.
- Generate a checksum-pinned DiscPanel dependency manifest covering 53 Fabric, Quilt, Babric, Legacy Fabric, and Ornithe runtime profiles, including only the version-compatible OSL modules used by each Ornithe generation.
- Deploy and remotely verify the initial 75 pinned runtime-dependency placements across the 53 dependency-bearing DiscPanel profiles, reusing 11 already-exact files, staging the other 64 without starting a server, and finish with all 99 profiles stopped and autostart disabled.
- Add a manifest-derived, resumable DiscPanel server-smoke matrix runner that resolves the release once, audits the complete stopped/autostart state before and after every sequential runtime, accepts resume evidence only for the exact candidate, and writes a sanitized aggregate result; all 99 live profiles pass its no-start preflight.
- Add a manifest-derived, resumable DiscPanel two-client live-Plex matrix runner with exact version/SHA/evidence resume, full-fleet stopped-state audits, explicit apply confirmation, private-X enforcement, and sanitized aggregate results. Harden the single-runtime gate against Gradle-detached clients by terminating only processes naming its unique session directory and rejecting evidence while any such process remains.
- Pass exact-current Beta 1.7.3 Babric live-Plex acceptance with two private-X production clients, synchronized rendered and decoder-trace audio, transactional stopped-server `ops.txt` operator injection, exact operator-file restoration, and clean whole-fleet teardown.
- Reserve DiscPanel's container TCP 25575 RCON port from game-port allocation, repair the stopped Forge 1.12.2 profile to 25664 while preserving its private environment, and verify separate public game/localhost RCON Docker bindings before passing an exact-artifact two-client live-Plex capture with 0.997119 rendered correlation, approximately 21 ms skew, no bad blocks, and no silence.
- Isolate each DiscPanel live gate from the matrix wrapper's terminal process group so `Ctrl-C` signals the gate shell and waits for transactional cleanup, detect terminal connection/OpenAL route failures immediately, remove only orphaned private live-gate null sinks while holding the runtime lock, and allow only the expected server-closed disconnect after accepted playback and clean teardown when resuming evidence.
- Pass the exact 1.16.5 Fabric artifact through a two-client live-Plex capture with 0.994278 rendered correlation, approximately 12 ms skew, no bad blocks, and no silence after separately classifying an OpenAL/PipeWire broken-stream client timeout as host audio-route evidence rather than mod chunk-order failure.
- Route modern OpenAL's ALSA backend through ALSA's Pulse plugin to the private null sink instead of addressing a Pulse-created sink as a native PipeWire node, then pass the exact 1.16.5 Quilt artifact with two private-X clients, 0.999432 rendered correlation, approximately 14 ms skew, no bad blocks, no silence, and clean fleet teardown.
- Isolate each live-client gate from the active desktop audio graph with a private PipeWire core, policy-only WirePlumber instance, Pulse-compatible socket, and exact-PID silent sink keepalives. The gate proves both virtual sinks are routed and running before it starts Minecraft, retains its private audio logs, and removes every daemon, socket directory, stream, and sink during transactional teardown.
- Retain session D-Bus access only for RTKit so the private PipeWire data loops receive realtime scheduling, and add six seconds of wall-capture grace without lowering the required active-program duration or any correlation, divergence, silence, or timing threshold. Exact-current 1.19.2 and 1.20 Forge repeats then passed at 0.998665 and 0.997991 rendered correlation with zero bad blocks or silence; the prior non-realtime divergence and content-dependent short-duration results remain rejected harness evidence, not mod failures.
- Prevent a real-Plex track transition from invalidating the fixed-lag live-audio oracle: wait until both clients have matching started channel/manifest generations, discard a recording if either generation changes, and retry at most three times without relaxing any skew, correlation, silence, skip, or reorder threshold. The exact 1.20.2 Fabric repeat held one generation for 36.7 analyzed seconds at 0.999374 correlation with zero bad blocks or silence.
- Extend exact-current two-client live-Plex acceptance through all sixteen 1.20.1, 1.20.2, 1.20.3, and 1.20.4 Fabric, Quilt, Forge, and NeoForge runtimes, retaining strict timing and cleanup thresholds while distinguishing hello suppression from a genuinely mod-free client launch.
- Reaccept the exact-current 1.16.5 Forge artifact through the isolated two-client live-Plex gate with 30.9 seconds of rendered audio at 0.996196 correlation, approximately 5 ms lag, zero silence, zero startup settling, and clean fleet teardown; retain the prior exact-current 1.18.2 Fabric and Quilt passes at 0.993650 and 0.999392 correlation.
- Make live-client server transactions recoverable after SIGKILL or execution-host loss with a credential-free durable journal for only the changed property values, prior `ONLINE_MODE` state, matching mod states, and isolated world. Automatically stop and restore an orphan before the next row, and cap each Gradle/client JVM at a configurable 1536 MiB after a 9.7 GiB no-swap host OOM; the bounded retry passed at a 7.8 GiB measured peak.
- Prevent jointly inaudible Pulse-monitor settling noise at the start of a capture from being classified as an active skipped/reordered region while retaining one-sided audible delays, active divergence, duration loss, and every existing timing fault as release failures.
- Preserve sanitized aggregate matrix evidence when the DiscPanel API becomes unreachable during the final fleet audit, marking the final state unproven before propagating the infrastructure failure.
- Complete the exact final-artifact DiscPanel server matrix across all 99 manifest-derived runtimes: every profile reached Minecraft readiness, initialized Jammarr 1.1.0, connected to the configured live Plex server, and returned cleanly to STOPPED. A separate no-start audit then rehashed and preflighted all 99 remote artifacts with zero failures and autostart disabled throughout.
- Add an isolated DiscPanel live-client gate for the final Forge 1.7.10 JAR. It temporarily disables unrelated server mods, bootstraps a unique world with the canonical live-Plex configuration, uses a unique temporary operator, runs two production clients on private X servers and separate null audio sinks, compares rendered and pre-backend PCM, and restores the config, properties, mods, operator list, world, server state, and all 99-profile stopped/autostart invariant. The final run captured 30.9 seconds with rendered correlation 0.998699, 32 ms skew, no bad blocks, and no silence.
- Fix production Legacy Fabric dependency resolution after live gates proved that the per-version Maven aggregate is descriptor-only and its raw module artifacts are not the canonical installable distribution. Pin the official 1.13.2 CurseForge bundle containing its nested runtime modules, and explicitly allow its guarded migration to disable the previously staged aggregate/module records as rollback copies.
- Detect terminal Minecraft process failures such as startup Mixin errors and `mc-server-runner` completion immediately instead of waiting for DiscPanel's stale `STARTING` state to reach the ten-minute timeout.
- Anchor DiscPanel smoke evidence to the newly started container segment so a rotated log window cannot replay a completed historical failure—or historical success—as evidence for the current run.
- Bound private live-client rendering to 854x480 at 30 FPS and mute every non-Music sound category so world-local effects cannot contaminate the two-client synchronization oracle. For modern clients, compare the existing pre-catch-up backend-ready timestamps instead of the later `channel_started` log instant, preventing unequal decoder-discard work from becoming a false timing failure while retaining the 25 ms program-origin limit and every rendered-audio threshold.
- Record both private clients through one four-channel master monitor and split their stereo pairs after capture, preventing independent Pulse recorder xruns from inserting unequal silence and masquerading as audio skips or reordering. Exact repeats of the three disputed rendered-audio rows passed with zero bad blocks, and ForgeGradle 7.0.36 repairs the separate 1.21.1 Forge launcher-metadata failure; the corrected target also passes its full two-client live-Plex gate.
- Harden sustained mixed-client audio qualification after a 120-cycle run localized a one-second gap to the private host graph while both Jammarr PCM feeds remained intact. Modern OpenAL now uses the qualified ALSA-Pulse route, an exact-PID monitor drain keeps the shared four-channel graph active between captures, buffer starvation and broken streams fail immediately, and artifact-free clients close normally before private-X teardown.
- Add a cross-window shared-clock continuity auditor that correlates the full pseudo-random marker-identity envelope and rejects persistent rendered phase steps hidden beneath the per-window skew ceiling. It can compare bounded decoder feeds to distinguish an after-decode OpenAL/Pulse shift from transport or chunk-order faults without treating either result as a release pass.
- Shard the 91-profile artifact-free-client and 99-profile unmodded-server compatibility matrices across four isolated lanes while grouping every loader for one Minecraft/cache family. Shared/exclusive runtime locks keep audible gates globally isolated, per-destination cache locks make concurrent verified fallback resolution safe, failed lanes retain bounded cleanup, and qualification-generation evidence roots plus behavior-specific resume validation prevent stale proof from reaching the read-only full-manifest completion summary.
- Give the reverse-compatibility connection relay a bounded 60-second startup allowance under four-lane pressure, a live-JVM ten-minute cold-client readiness ceiling, and immediate signal-driven teardown, preventing cold Quilt transforms, slow process creation, and a blocked accept loop from becoming false failures or prolonged cleanup.
- Accept both Mojang server-object hosts emitted by the authoritative version manifest when verifying reverse-compatibility evidence. Older releases such as 1.6.4 legitimately resolve from `launcher.mojang.com`, while newer releases use `piston-data.mojang.com`; both remain pinned to the manifest's exact SHA-1 object path and non-Mojang hosts remain rejected.
- Negotiate Legacy Fabric 1.6.4 server capability through a nonce-bound vanilla chat command before enabling Jammarr custom payloads. This replaces the incorrect assumption that every completed login supports Jammarr, lets clients on unmodded servers keep the unsupported-server screen open without disconnecting, hides the capability response, and preserves normal Jammarr-server negotiation.
- Require an explicit remote Jammarr channel-registration event on Legacy Fabric 1.8.9. Its generic post-join `canSend` query reported true even against vanilla, so the client now begins unavailable and enables custom payloads only after the server advertises the channel.
- Bound Forge-family development-client bootstrap during sharded qualification. Forge 49.0.2 can begin parallel model baking before its own client config attaches when several software-rendered development clients saturate the host, producing development-only missing models and a chunk-compile crash even though the same optional-client lifecycle passes alone. Only Forge/NeoForge suppressed-hello development clients now share a bootstrap lock and report four processors; independent servers and exact vanilla clients remain parallel.
- Serialize complete Forge runtime gates around ForgeGradle Mavenizer's cross-project cache. Resource locks already isolate Minecraft-version workspaces, but newer Forge projects share one global launcher manifest; concurrent writers exposed a truncated 163,841-byte JSON file to 1.21.5 Forge while another lane was replacing it. The next completed download matched Mojang's 274,725-byte manifest exactly, the unchanged 1.21.5 gate passed alone, and its four-lane retry passed behind the cache lock. Fabric, Quilt, NeoForge, and exact vanilla work remain concurrent.
- Fail closed before Minecraft advertises readiness on 26.1.2 and 26.2 Fabric, Quilt, Forge, and NeoForge. Fabric/Quilt and NeoForge validate and install the world-scoped Jammarr configuration during their server-starting event; Forge 64/65 does so at `ServerAboutToStartEvent` because its nominal starting event follows `Done`. Playback-coordinator construction remains at server-started. Exact 26.1.2 and 26.2 gates for Fabric, Forge, and NeoForge reject an invalid `plexUrl` before readiness and then pass valid startup plus clean teardown.
- Complete the 91-runtime artifact-free-client compatibility matrix. Every exact Mojang client joined twice on private X, supplied authoritative player chat, remained connected without Jammarr, left listener/Plex accounting unchanged, and cleaned up its processes and port. A current-source 26.1.2 Forge refresh replaced its pre-fix pointer, and a separate evidence-only verifier reaccepted all 91 rows with zero failures.
- Gate Minecraft 1.16-through-1.19 artifact-free client login on the current client's completed initial resource-atlas load instead of a fixed startup delay. The loopback relay fails closed on timeout or early exit, records the readiness release in its attestation, and lets resume retain only unaffected non-relay evidence from the prior schema.
- Apply the same current-client resource-atlas readiness boundary to the reverse modded-client/unmodded-server gate for Minecraft 1.16 through 1.19. A loopback relay prevents the fast disposable server from delivering chunks while vanilla's `ModelManager` is still unapplied, and reverse-matrix resume now rejects older affected evidence that lacks the deferred-release marker.
- Paste the artifact-free client's chat proof atomically through its private-X clipboard after the chat widget settles. This prevents software-rendering contention from dropping an early prefix while retaining authoritative server-side verification of the complete player-originated marker.
- Store each artifact-free matrix execution in an immutable attempt directory and atomically promote only a zero-exit, fully verified attempt. Resume pointers are runtime/version/duration/path scoped, failed attempts cannot combine with older success files, and legacy flat evidence must match its own embedded connection-release contract.
- Stabilize artifact-free client startup under parallel qualification. The dedicated gate now waits for each private `setsid` process group before judging liveness, and exact Minecraft 1.16 through 1.19 clients connect through a bounded loopback-only relay after their initial resource reload so a fast local server cannot deliver chunks before vanilla's baked model registry is ready.
- Keep disposable official-server compatibility fixtures lightweight with flat structure-free worlds, disabled entity spawning, and four active processors. Reap an exited server leader instead of treating its zombie as a live process group, while still failing non-zero shutdown, retained processes, or an open game port.
- Preserve reverse-compatibility functional evidence separately from fixture cleanup, bound an official disposable server's graceful-stop allowance, and explicitly record a forced timeout cleanup without repeating a client that already proved join, unsupported-server UI, and connection duration. Acceptance still rejects any retained client/server/private-X process or open game port.
- Keep the Babric Beta 1.7.3 client connected to an unmodified server by replacing its unsafe packet-254 first hello with a nonce-bound vanilla-chat capability probe. Only a hidden response from a Jammarr server enables StationAPI custom traffic; vanilla's unknown-command reply is suppressed locally and opens the unsupported-server UI.
- Correct audible phase drift after decoding on Minecraft 1.16.5 and newer by measuring OpenAL's submitted, queued, played, and device-latency frames on the sound thread. Cold devices calibrate while muted, learned latency survives normal track and sound-reload boundaries, sustained same-direction drift beyond 40 ms triggers bounded recovery, and unsupported backends retain the prior wall-clock fail-safe.
- Make invalid-configuration acceptance deterministic across loader lifecycle ordering: allow the bounded same-thread server-started callback after the vanilla `Done` log, while still requiring the exact redacted rejection, closed port, and complete process cleanup before any client starts.
- Bind runtime acceptance to manifest-assigned game and RCON ports even when persistent Gradle run directories retain older values, restoring the original server properties after each gate and rejecting mismatched client attestations.
- Launch legacy artifact-free clients from verified Prism metadata that contains native-only classifier containers such as `jinput-platform`, excluding those containers from the classpath while still verifying and extracting their platform archive.
- Retry bounded transient transport failures while populating the isolated vanilla-client cache without retrying or accepting any completed size/SHA-1 mismatch.
- Keep pure vanilla 1.12.2 clients interactive on Forge with a connection-local outbound filter that removes only Forge's reach/swim snapshots from packet-private copies, without changing global attributes or packets sent to Forge clients.
- Make exact legacy-client acceptance deterministic under private X: prevent focus loss from opening Minecraft's pause menu, stop LWJGL 2 clients without destroying their X drawable, and use the working inherited console for the RFG 1.12.2 server whose RCON socket closes after authentication.
- Complete exact-current two-client live-Plex acceptance across all 99 DiscPanel runtime profiles. Pin ForgeGradle 7.0.36 on the Gradle-9 Forge 26.1.2 build, guard every Gradle-9 Forge target against the affected launcher-metadata plugin version, pass the final 26.1.2 Forge and 26.2 Fabric/Forge/NeoForge rows with zero bad blocks, and finish with a 99-of-99 exact-evidence resume audit and the complete fleet stopped.

## 1.0.2 - 2026-08-28

- Give Forge 1.7.10 PCM a single direct OpenAL queue owner instead of routing it through Paulscode's competing stream-maintenance and raw-feed paths, prevent stopped queues from replaying stale PCM, keep a larger backend reserve across ordinary client hitches, and extend rendered legacy audio verification beyond the former ten-second window.
- Prevent every client backend from starting before its authoritative chunk boundary or without enough decoded PCM to discard startup delay, trim late starts at PCM-sample alignment instead of whole MP3 frames, reject incomplete catch-up, and rebuild on every authoritative timeline change so old and replacement audio cannot overlap.
- Make the rendered-audio oracle reject deliberately injected early, late, replayed, reordered, and overlapping PCM instead of relying only on cadence and silence.
- Fix the Forge 1.7.10 handshake-timeout crash reported on Uranium by removing pending state before disconnect callbacks can fire, and extend the production hello deadline from five to sixty seconds.
- Apply the sixty-second hello policy to every Fabric and Quilt runtime while retaining an acceptance-only short timeout for deterministic missing-client tests.
- Prefer the Plex music library titled `Music` when `musicLibrary` is blank, and fail closed when unscoped metadata, playlists, native radio, or sonic results do not belong to the selected library.
- Clarify which playback and station modes work without Plex Pass, including the optional metadata fallback and Adventure's Sonic-only requirement.
- Start decoded audio with enough of the server's scheduling lead remaining, wait for measured clock synchronization, skip PCM elapsed during asynchronous channel creation, publish backend-ready channel state atomically across threads to prevent duplicate orphan playback, and give newly aligned channels a short correction grace instead of accepting startup skew or same-tick recovery churn.
- Bootstrap time synchronization with ten rapid samples, retain the lowest-round-trip measurement without blending in asymmetric server-thread delay, and ignore unmeasured playback-state timestamps so a slow first response cannot misalign late joiners.
- Tolerate transient chunk-delivery delays without prematurely rebuffering audio that the sound backend still has queued, while retaining explicit forced-underrun recovery coverage.
- Coalesce sound-thread volume updates so stale per-tick commands cannot delay a local volume change under load.
- Bound temporary decoder waits on the sound executor so a withheld compressed window cannot stall volume, stop, reload, or recovery commands.
- Reserve one complete low-bitrate chunk window in server playback-lead flow control, and avoid backend state queries while a streaming read is waiting for that bounded delivery window.
- Add concise hover descriptions to every main-menu button across modern and Forge 1.7.10 clients, and require two seeds before the legacy Sonic Mix action becomes available.
- Add deterministic marked-audio, two-client timing, pre-backend legacy PCM classification, and isolated network-impairment acceptance tooling.
- Start synchronized PCM capture only after both clients' latest audio states remain PLAYING, including when a slow late join overlaps leader recovery.
- Isolate shared-core build outputs per Gradle root so incremental cross-version release verification cannot reuse incompatible binary test metadata.
- Retain protocol 5, saved-data schema 4, and the complete 16-artifact / 21-runtime compatibility matrix.

## 1.0.1 - 2026-08-27

- Fix Forge 1.7.10 clients crashing in LWJGL2/OpenAL after Forge starts overlapping sound-engine reloads, including the `nalGetSourcei` and `nalSourcef` native-link failures seen on Linux.
- Let the initial 1.7.10 sound loader settle during normal startup, recover safely from an unusable or reloaded sound engine, and restore vanilla music scheduling when Jammarr no longer owns playback.
- Replace fragile modern SoundManager/SoundEngine accessor Mixins with runtime type-based field lookup, fixing Forge and NeoForge audio startup across mapped and production runtimes.
- Retain protocol 5, the existing server configuration format, and the complete 16-artifact / 21-runtime compatibility matrix.

## 1.0.0 - 2026-08-25

- Initial Jammarr release.
